---
phase: 02-authentication
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/package.json
  - apps/frontend/src/main.tsx
  - apps/frontend/src/index.css
  - apps/frontend/vite.config.ts
  - apps/frontend/src/lib/utils.ts
  - apps/frontend/src/lib/axios.ts
  - apps/frontend/src/stores/auth-store.ts
  - apps/frontend/src/components/layouts/auth-layout.tsx
  - apps/frontend/src/components/layouts/app-layout.tsx
  - apps/frontend/src/components/layouts/sidebar.tsx
  - apps/frontend/src/components/layouts/private-route.tsx
  - apps/frontend/src/components/ui/initials-avatar.tsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "shadcn/ui is initialized with components.json in apps/frontend and @theme block in index.css"
    - "Zustand auth store holds user, accessToken, isAuthenticated, isHydrating state"
    - "Axios interceptor attempts silent refresh on 401, queues concurrent requests, redirects to /login on refresh failure"
    - "PrivateRoute shows spinner during hydration and redirects to /login?next= when unauthenticated"
    - "AuthLayout renders centered card with logo for unauthenticated pages"
    - "AppLayout renders sidebar with navigation items, user info, and logout button"
    - "InitialsAvatar renders deterministic colored circle from user ID"
    - "Vite proxy forwards /auth/* and /users/* requests to backend"
  artifacts:
    - path: "apps/frontend/src/stores/auth-store.ts"
      provides: "Auth state management"
      exports: ["useAuthStore"]
    - path: "apps/frontend/src/lib/axios.ts"
      provides: "Configured axios instance with silent refresh interceptor"
      exports: ["api"]
    - path: "apps/frontend/src/components/layouts/auth-layout.tsx"
      provides: "Centered card layout for login/signup"
      exports: ["AuthLayout"]
    - path: "apps/frontend/src/components/layouts/app-layout.tsx"
      provides: "Sidebar + main content layout"
      exports: ["AppLayout"]
    - path: "apps/frontend/src/components/layouts/sidebar.tsx"
      provides: "Navigation sidebar with user info and logout"
      exports: ["Sidebar"]
    - path: "apps/frontend/src/components/layouts/private-route.tsx"
      provides: "Auth guard wrapper with hydration spinner"
      exports: ["PrivateRoute"]
    - path: "apps/frontend/src/components/ui/initials-avatar.tsx"
      provides: "Deterministic initials avatar component"
      exports: ["InitialsAvatar"]
  key_links:
    - from: "apps/frontend/src/lib/axios.ts"
      to: "apps/frontend/src/stores/auth-store.ts"
      via: "useAuthStore.getState() in interceptor"
      pattern: "useAuthStore\\.getState"
    - from: "apps/frontend/src/components/layouts/private-route.tsx"
      to: "apps/frontend/src/stores/auth-store.ts"
      via: "useAuthStore() hook"
      pattern: "useAuthStore"
    - from: "apps/frontend/src/components/layouts/sidebar.tsx"
      to: "apps/frontend/src/stores/auth-store.ts"
      via: "useAuthStore() for user info and logout"
      pattern: "useAuthStore"
    - from: "apps/frontend/vite.config.ts"
      to: "backend API"
      via: "Vite proxy configuration"
      pattern: "proxy.*localhost.*3000"
---

<objective>
Initialize shadcn/ui, install all frontend packages, create the Zustand auth store, axios instance with silent refresh interceptor, all layout components (AuthLayout, AppLayout, Sidebar, PrivateRoute), the InitialsAvatar component, and configure Vite proxy for API calls.

Purpose: Establishes the complete frontend infrastructure that auth pages (Plan 04) build upon. The auth store, axios interceptor, layouts, and routing wrappers must all exist before pages can be created.

Output: Working frontend shell with shadcn components available, auth state management, API client with token refresh, and all layout components ready for page content.
</objective>

<execution_context>
@C:/Users/minhNN03/Dev/CClone LMS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhNN03/Dev/CClone LMS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-UI-SPEC.md
@.planning/phases/02-authentication/02-VALIDATION.md

<interfaces>
<!-- Existing frontend files to build upon -->

From apps/frontend/src/main.tsx:
```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from '@/App';
import '@/index.css';
```

From apps/frontend/src/index.css:
```css
@import "tailwindcss";
```

From apps/frontend/vite.config.ts:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import { resolve } from 'path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': resolve(__dirname, './src'),
    },
  },
});
```

From apps/frontend/src/App.tsx (current scaffold — will be replaced in Plan 04):
```typescript
import { createBrowserRouter, RouterProvider } from 'react-router';
// Bare scaffold with single "/" route showing "CClone LMS / Phase 1 — Scaffold OK"
export const App = () => <RouterProvider router={router} />;
```

From apps/frontend/package.json:
```json
{
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router": "^7.14.0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui, install all frontend packages, configure Vite proxy and fonts</name>
  <files>
    apps/frontend/package.json,
    apps/frontend/src/main.tsx,
    apps/frontend/src/index.css,
    apps/frontend/vite.config.ts,
    apps/frontend/src/lib/utils.ts,
    apps/frontend/components.json
  </files>
  <read_first>
    apps/frontend/package.json,
    apps/frontend/src/main.tsx,
    apps/frontend/src/index.css,
    apps/frontend/vite.config.ts,
    .planning/phases/02-authentication/02-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Initialize shadcn/ui.**

Run from inside `apps/frontend/` directory:
```bash
cd apps/frontend && npx shadcn@latest init
```

When prompted, use these values per UI-SPEC.md:
- Style: Default
- Base color: Neutral (shadcn will generate CSS variables)
- CSS variables: Yes
- React Server Components: No
- Components path: `src/components/ui`
- Utils path: `src/lib/utils.ts`

If the CLI does not prompt interactively, try:
```bash
npx shadcn@latest init --defaults
```

After init completes, verify:
- `apps/frontend/components.json` exists
- `apps/frontend/src/lib/utils.ts` exists (contains `cn()` function)
- `apps/frontend/src/index.css` has been updated with `@theme { ... }` block by shadcn

**Step 2: Install shadcn components.**

```bash
cd apps/frontend && npx shadcn@latest add button input label card alert form separator sonner
```

This copies component source files into `apps/frontend/src/components/ui/`.

**Step 3: Install frontend packages.**

From repo root:
```bash
pnpm --filter frontend add @tanstack/react-query zustand react-hook-form @hookform/resolvers zod axios lucide-react @fontsource/inter sonner
pnpm --filter frontend add -D @tanstack/react-query-devtools
```

Note: `sonner` is the toast library used by shadcn's toast component. `@hookform/resolvers` is the bridge between react-hook-form and zod.

**Step 4: Update `apps/frontend/src/main.tsx`.**

Add font imports and TanStack Query provider. The updated file should be:

```typescript
import '@fontsource/inter/400.css';
import '@fontsource/inter/600.css';

import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { Toaster } from 'sonner';

import { App } from '@/App';
import '@/index.css';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,  // 5 minutes
      retry: 1,
    },
  },
});

const rootElement = document.getElementById('root');
if (!rootElement) {
  throw new Error('Root element not found');
}

createRoot(rootElement).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
      <Toaster position="bottom-right" richColors closeButton />
    </QueryClientProvider>
  </StrictMode>,
);
```

**Step 5: Update `apps/frontend/vite.config.ts`.**

Add Vite proxy configuration to forward API requests to the NestJS backend:

```typescript
import tailwindcss from '@tailwindcss/vite';
import react from '@vitejs/plugin-react';
import { resolve } from 'path';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': resolve(__dirname, './src'),
    },
  },
  server: {
    proxy: {
      '/auth': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
      '/users': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
    },
  },
});
```

This makes frontend API calls to `/auth/*` and `/users/*` same-origin during development, which means cookies are sent automatically without needing `withCredentials: true` for same-origin requests. However, we still set `withCredentials: true` on the axios instance for robustness.

**Step 6: Add Inter font family to CSS.**

After shadcn init has updated `apps/frontend/src/index.css`, add the Inter font-family declaration. The file should start with:

```css
@import "tailwindcss";

/* shadcn @theme block will be here — do NOT remove it */

body {
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
}
```

Append the `body` rule AFTER the existing content (including shadcn's `@theme` block). Do not remove any content that shadcn added.
  </action>
  <verify>
    <automated>cd apps/frontend && test -f components.json && test -f src/lib/utils.ts && test -f src/components/ui/button.tsx && node -e "const p = require('./package.json'); const deps = {...p.dependencies, ...p.devDependencies}; const required = ['@tanstack/react-query','zustand','react-hook-form','zod','axios','lucide-react','@fontsource/inter','sonner']; const missing = required.filter(r => !deps[r]); if(missing.length){console.error('Missing:',missing);process.exit(1)}else{console.log('All packages present')}"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/components.json` exists (shadcn initialized)
    - `apps/frontend/src/lib/utils.ts` exists and contains `export function cn`
    - `apps/frontend/src/components/ui/button.tsx` exists (shadcn button installed)
    - `apps/frontend/src/components/ui/input.tsx` exists
    - `apps/frontend/src/components/ui/card.tsx` exists
    - `apps/frontend/src/components/ui/alert.tsx` exists
    - `apps/frontend/src/components/ui/form.tsx` exists
    - `apps/frontend/src/index.css` contains `@theme` block (added by shadcn)
    - `apps/frontend/src/index.css` contains `font-family: 'Inter'`
    - `apps/frontend/src/main.tsx` contains `import '@fontsource/inter/400.css'` and `import '@fontsource/inter/600.css'`
    - `apps/frontend/src/main.tsx` contains `QueryClientProvider` and `Toaster`
    - `apps/frontend/package.json` dependencies contain `@tanstack/react-query`, `zustand`, `react-hook-form`, `zod`, `axios`, `lucide-react`, `@fontsource/inter`, `sonner`
    - `apps/frontend/vite.config.ts` contains `proxy` configuration with `/auth` and `/users` targets pointing to `localhost:3000`
  </acceptance_criteria>
  <done>shadcn/ui initialized with all required components (button, input, label, card, alert, form, separator, sonner). All frontend packages installed. Inter font loaded and applied. TanStack Query provider wrapping app. Toaster configured at bottom-right per UI-SPEC. Vite proxy routes /auth and /users to backend.</done>
</task>

<task type="auto">
  <name>Task 2: Create Zustand auth store and axios instance with silent refresh interceptor</name>
  <files>
    apps/frontend/src/stores/auth-store.ts,
    apps/frontend/src/lib/axios.ts
  </files>
  <read_first>
    apps/frontend/package.json,
    .planning/phases/02-authentication/02-CONTEXT.md,
    .planning/phases/02-authentication/02-RESEARCH.md
  </read_first>
  <action>
**1. Create `apps/frontend/src/stores/auth-store.ts`.**

Per D-01: access token stored in Zustand memory only (never localStorage, never sessionStorage).
Per D-18: `isHydrating` starts as `true` to prevent flash of redirect.

```typescript
import { create } from 'zustand';

interface AuthUser {
  id: string;
  email: string;
  name: string | null;
  role: string;
  timezone: string;
}

interface AuthState {
  user: AuthUser | null;
  accessToken: string | null;
  isAuthenticated: boolean;
  isHydrating: boolean;
  setAuth: (user: AuthUser, accessToken: string) => void;
  setAccessToken: (token: string) => void;
  setUser: (user: AuthUser) => void;
  clearAuth: () => void;
  setHydrating: (value: boolean) => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  accessToken: null,
  isAuthenticated: false,
  isHydrating: true,

  setAuth: (user: AuthUser, accessToken: string) =>
    set({ user, accessToken, isAuthenticated: true, isHydrating: false }),

  setAccessToken: (token: string) =>
    set({ accessToken: token }),

  setUser: (user: AuthUser) =>
    set({ user }),

  clearAuth: () =>
    set({ user: null, accessToken: null, isAuthenticated: false, isHydrating: false }),

  setHydrating: (value: boolean) =>
    set({ isHydrating: value }),
}));
```

**2. Create `apps/frontend/src/lib/axios.ts`.**

Per D-03: silent refresh via axios response interceptor. On 401, attempt `/auth/refresh` before retrying original request. If refresh fails, clear Zustand store and redirect to `/login?next={encodedPath}`.

The interceptor must handle concurrent 401s using a queue pattern to prevent multiple simultaneous refresh calls.

```typescript
import axios from 'axios';
import type { AxiosError, InternalAxiosRequestConfig } from 'axios';

import { useAuthStore } from '@/stores/auth-store';

export const api = axios.create({
  baseURL: '/',
  headers: {
    'Content-Type': 'application/json',
  },
  withCredentials: true,  // Send cookies on all requests (needed for refresh)
});

// Request interceptor: attach access token from Zustand store
api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// Response interceptor: silent refresh on 401
let isRefreshing = false;
let failedQueue: Array<{
  resolve: (token: string) => void;
  reject: (err: unknown) => void;
}> = [];

const processQueue = (error: unknown, token: string | null): void => {
  failedQueue.forEach((promise) => {
    if (token) {
      promise.resolve(token);
    } else {
      promise.reject(error);
    }
  });
  failedQueue = [];
};

api.interceptors.response.use(
  (response) => response,
  async (error: AxiosError) => {
    const originalRequest = error.config as InternalAxiosRequestConfig & {
      _retry?: boolean;
    };

    // Only intercept 401 errors, not on retry, and not on the refresh endpoint itself
    if (
      error.response?.status !== 401 ||
      originalRequest._retry ||
      originalRequest.url === '/auth/refresh'
    ) {
      return Promise.reject(error);
    }

    if (isRefreshing) {
      // Queue this request while refresh is in progress
      return new Promise<string>((resolve, reject) => {
        failedQueue.push({ resolve, reject });
      }).then((token) => {
        originalRequest.headers.Authorization = `Bearer ${token}`;
        return api(originalRequest);
      });
    }

    originalRequest._retry = true;
    isRefreshing = true;

    try {
      const { data } = await api.post('/auth/refresh');
      const newToken = data.data.accessToken as string;
      const newUser = data.data.user;

      useAuthStore.getState().setAuth(newUser, newToken);
      processQueue(null, newToken);

      originalRequest.headers.Authorization = `Bearer ${newToken}`;
      return api(originalRequest);
    } catch (refreshError) {
      processQueue(refreshError, null);
      useAuthStore.getState().clearAuth();

      // Redirect to login with current path as next destination
      const currentPath = window.location.pathname + window.location.search;
      window.location.href = `/login?next=${encodeURIComponent(currentPath)}`;

      return Promise.reject(refreshError);
    } finally {
      isRefreshing = false;
    }
  },
);
```

Key implementation details:
- `baseURL: '/'` — uses relative URLs so Vite proxy handles routing to backend
- `withCredentials: true` — ensures httpOnly cookie is sent on requests (needed for cross-origin in production, harmless for same-origin via proxy)
- Request interceptor reads token from `useAuthStore.getState()` (outside React component — this is the Zustand pattern for non-React code)
- Response interceptor checks `originalRequest.url === '/auth/refresh'` to prevent infinite loop (the refresh endpoint itself returning 401 should NOT trigger another refresh attempt)
- `failedQueue` pattern handles concurrent 401s — only one refresh call happens, all other pending 401s wait and retry with the new token
- On refresh failure: `clearAuth()` resets store, then redirect to `/login?next={currentPath}` per D-17
  </action>
  <verify>
    <automated>cd apps/frontend && node -e "const fs=require('fs');const s=fs.readFileSync('src/stores/auth-store.ts','utf8');const a=fs.readFileSync('src/lib/axios.ts','utf8');let ok=true;if(!s.includes('isHydrating: true')){console.error('Missing isHydrating: true');ok=false}if(!s.includes('useAuthStore')){console.error('Missing useAuthStore export');ok=false}if(!a.includes('isRefreshing')){console.error('Missing isRefreshing in axios');ok=false}if(!a.includes('failedQueue')){console.error('Missing failedQueue');ok=false}if(!a.includes('/auth/refresh')){console.error('Missing refresh URL');ok=false}if(!a.includes('clearAuth')){console.error('Missing clearAuth on failure');ok=false}if(ok)console.log('Auth store and axios interceptor verified')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/stores/auth-store.ts` exports `useAuthStore`
    - `apps/frontend/src/stores/auth-store.ts` contains `isHydrating: true` as initial state (per D-18)
    - `apps/frontend/src/stores/auth-store.ts` contains `setAuth`, `setAccessToken`, `setUser`, `clearAuth`, `setHydrating` actions
    - `apps/frontend/src/stores/auth-store.ts` interface `AuthUser` contains `id`, `email`, `name`, `role`, `timezone` fields
    - `apps/frontend/src/lib/axios.ts` exports `api` axios instance
    - `apps/frontend/src/lib/axios.ts` contains `withCredentials: true`
    - `apps/frontend/src/lib/axios.ts` contains request interceptor reading `useAuthStore.getState().accessToken`
    - `apps/frontend/src/lib/axios.ts` contains response interceptor with `isRefreshing` flag and `failedQueue` array
    - `apps/frontend/src/lib/axios.ts` contains check `originalRequest.url === '/auth/refresh'` to prevent infinite loop
    - `apps/frontend/src/lib/axios.ts` contains `window.location.href = '/login?next='` with `encodeURIComponent` on refresh failure
    - Neither file contains `localStorage` or `sessionStorage` (per D-01 — XSS protection)
  </acceptance_criteria>
  <done>Zustand auth store created with user/token state and isHydrating flag (per D-01, D-18). Axios instance created with request interceptor (attach Bearer token per D-01) and response interceptor (silent refresh per D-03, concurrent request queue, infinite loop prevention, redirect on failure per D-17). No token persisted to storage — memory only.</done>
</task>

<task type="auto">
  <name>Task 3: Create layout components — AuthLayout, AppLayout, Sidebar, PrivateRoute, InitialsAvatar</name>
  <files>
    apps/frontend/src/components/layouts/auth-layout.tsx,
    apps/frontend/src/components/layouts/app-layout.tsx,
    apps/frontend/src/components/layouts/sidebar.tsx,
    apps/frontend/src/components/layouts/private-route.tsx,
    apps/frontend/src/components/ui/initials-avatar.tsx
  </files>
  <read_first>
    apps/frontend/src/stores/auth-store.ts,
    apps/frontend/src/lib/axios.ts,
    apps/frontend/src/components/ui/button.tsx,
    apps/frontend/src/components/ui/card.tsx,
    apps/frontend/src/lib/utils.ts,
    .planning/phases/02-authentication/02-UI-SPEC.md,
    .planning/phases/02-authentication/02-CONTEXT.md
  </read_first>
  <action>
**1. Create `apps/frontend/src/components/ui/initials-avatar.tsx`.**

Per UI-SPEC InitialsAvatar section and D-22:

```typescript
import { cn } from '@/lib/utils';

const PALETTE = [
  '#6366f1', // indigo-500
  '#8b5cf6', // violet-500
  '#0ea5e9', // sky-500
  '#14b8a6', // teal-500
  '#10b981', // emerald-500
  '#f59e0b', // amber-500
  '#f43f5e', // rose-500
  '#64748b', // slate-500
];

const getInitials = (displayName: string): string => {
  const parts = displayName.trim().split(/\s+/);
  if (parts.length >= 2) {
    return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
  }
  return displayName.slice(0, 2).toUpperCase();
};

const getColor = (userId: string): string => {
  const sum = userId.split('').reduce((acc, ch) => acc + ch.charCodeAt(0), 0);
  return PALETTE[sum % PALETTE.length];
};

interface InitialsAvatarProps {
  userId: string;
  displayName: string;
  size?: 'sm' | 'lg';
  className?: string;
}

export const InitialsAvatar = ({
  userId,
  displayName,
  size = 'sm',
  className,
}: InitialsAvatarProps) => {
  const initials = getInitials(displayName || '?');
  const bgColor = getColor(userId);

  const sizeClasses = size === 'sm'
    ? 'h-8 w-8 text-xs'     // 32px, 12px font
    : 'h-10 w-10 text-sm';  // 40px, 14px font

  return (
    <div
      role="img"
      aria-label={`${displayName}'s avatar`}
      className={cn(
        'flex items-center justify-center rounded-full font-semibold text-white select-none shrink-0',
        sizeClasses,
        className,
      )}
      style={{ backgroundColor: bgColor }}
    >
      {initials}
    </div>
  );
};
```

**2. Create `apps/frontend/src/components/layouts/private-route.tsx`.**

Per D-17 and D-18 from CONTEXT.md and UI-SPEC PrivateRoute section:

```typescript
import { Loader2 } from 'lucide-react';
import { Navigate, Outlet, useLocation } from 'react-router';

import { useAuthStore } from '@/stores/auth-store';

export const PrivateRoute = () => {
  const { isAuthenticated, isHydrating } = useAuthStore();
  const location = useLocation();

  if (isHydrating) {
    return (
      <div
        className="flex min-h-screen items-center justify-center bg-background"
        aria-label="Loading"
      >
        <Loader2 className="h-8 w-8 animate-spin text-primary" />
      </div>
    );
  }

  if (!isAuthenticated) {
    const next = encodeURIComponent(location.pathname + location.search);
    return <Navigate to={`/login?next=${next}`} replace />;
  }

  return <Outlet />;
};
```

**3. Create `apps/frontend/src/components/layouts/auth-layout.tsx`.**

Per UI-SPEC AuthLayout section and D-12:

```typescript
import { GraduationCap } from 'lucide-react';
import { Outlet } from 'react-router';

import { Card, CardContent } from '@/components/ui/card';

export const AuthLayout = () => {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background">
      <Card className="w-full max-w-[400px]">
        <CardContent className="px-8 py-12">
          <div className="mb-6 text-center">
            <GraduationCap className="mx-auto h-8 w-8 text-primary" />
            <p className="mt-1 text-sm font-semibold">CClone LMS</p>
          </div>
          <Outlet />
        </CardContent>
      </Card>
    </div>
  );
};
```

**4. Create `apps/frontend/src/components/layouts/sidebar.tsx`.**

Per UI-SPEC Sidebar section and D-16. The sidebar contains logo, nav items, user info, and logout button.

Per D-16 from CONTEXT.md: minimal sidebar — logo, user avatar + name, and logout button. BUT the UI-SPEC (which was approved) includes nav items (Dashboard, Courses, Assignments, Gradebook). Since the CONTEXT.md D-16 says "Nav items (Dashboard, Courses, etc.) are added by each subsequent phase as they build out" but the UI-SPEC section explicitly lists them with icons and routes, AND the RESEARCH.md D-12 says "Full sidebar nav skeleton built in Phase 2", include the nav items as the UI-SPEC specifies. The placeholder pages will be created in Plan 04.

```typescript
import {
  BarChart3,
  BookOpen,
  ClipboardList,
  GraduationCap,
  LayoutDashboard,
  LogOut,
  Settings,
} from 'lucide-react';
import { Link, useLocation } from 'react-router';

import { Button } from '@/components/ui/button';
import { Separator } from '@/components/ui/separator';
import { InitialsAvatar } from '@/components/ui/initials-avatar';
import { useAuthStore } from '@/stores/auth-store';
import { api } from '@/lib/axios';
import { cn } from '@/lib/utils';

interface NavItem {
  label: string;
  icon: React.ComponentType<{ className?: string }>;
  path: string;
}

const navItems: NavItem[] = [
  { label: 'Dashboard', icon: LayoutDashboard, path: '/dashboard' },
  { label: 'Courses', icon: BookOpen, path: '/courses' },
  { label: 'Assignments', icon: ClipboardList, path: '/assignments' },
  { label: 'Gradebook', icon: BarChart3, path: '/gradebook' },
];

export const Sidebar = () => {
  const location = useLocation();
  const { user, clearAuth } = useAuthStore();

  const handleLogout = async (): Promise<void> => {
    try {
      await api.delete('/auth/logout');
    } catch {
      // Logout should succeed client-side even if server call fails
    }
    clearAuth();
    window.location.href = '/login';
  };

  return (
    <aside className="flex h-full w-[256px] shrink-0 flex-col border-r border-border bg-card">
      {/* Logo */}
      <div className="flex items-center gap-2 border-b border-border px-4 py-6">
        <GraduationCap className="h-5 w-5 text-primary" />
        <span className="text-sm font-semibold">CClone LMS</span>
      </div>

      {/* Navigation */}
      <nav className="flex flex-1 flex-col gap-1 px-2 py-4">
        {navItems.map((item) => {
          const isActive = location.pathname === item.path ||
            location.pathname.startsWith(item.path + '/');
          const Icon = item.icon;

          return (
            <Link
              key={item.path}
              to={item.path}
              className={cn(
                'flex min-h-[44px] items-center gap-2 rounded-md px-2 text-sm transition-colors',
                isActive
                  ? 'border-l-2 border-primary bg-primary/10 text-primary'
                  : 'text-muted-foreground hover:bg-muted hover:text-foreground',
              )}
            >
              <Icon className={cn('h-4 w-4', isActive ? 'text-primary' : '')} />
              {item.label}
            </Link>
          );
        })}
      </nav>

      {/* User section */}
      <div className="border-t border-border px-2 py-4">
        <div className="flex items-center gap-2 rounded-md px-2 py-2">
          {user && (
            <InitialsAvatar
              userId={user.id}
              displayName={user.name ?? user.email}
              size="sm"
            />
          )}
          <div className="min-w-0 flex-1">
            <p className="truncate text-sm font-semibold text-foreground" style={{ maxWidth: 140 }}>
              {user?.name ?? 'User'}
            </p>
            <p className="truncate text-xs text-muted-foreground" style={{ maxWidth: 140 }}>
              {user?.email ?? ''}
            </p>
          </div>
          <Link to="/profile" aria-label="Account settings" className="ml-auto">
            <Settings className="h-4 w-4 text-muted-foreground hover:text-foreground" />
          </Link>
        </div>

        <Button
          variant="ghost"
          className="mt-1 w-full min-h-[44px] justify-start gap-2 text-sm text-destructive hover:text-destructive"
          onClick={handleLogout}
        >
          <LogOut className="h-4 w-4" />
          Log out
        </Button>
      </div>
    </aside>
  );
};
```

**5. Create `apps/frontend/src/components/layouts/app-layout.tsx`.**

Per UI-SPEC AppLayout section:

```typescript
import { Outlet } from 'react-router';

import { Sidebar } from '@/components/layouts/sidebar';

export const AppLayout = () => {
  return (
    <div className="flex h-screen overflow-hidden bg-background">
      <Sidebar />
      <main className="flex-1 overflow-y-auto p-6">
        <Outlet />
      </main>
    </div>
  );
};
```
  </action>
  <verify>
    <automated>cd apps/frontend && node -e "const fs=require('fs');const files=['src/components/layouts/auth-layout.tsx','src/components/layouts/app-layout.tsx','src/components/layouts/sidebar.tsx','src/components/layouts/private-route.tsx','src/components/ui/initials-avatar.tsx'];let ok=true;files.forEach(f=>{if(!fs.existsSync(f)){console.error('Missing:',f);ok=false}});if(ok)console.log('All layout files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/components/ui/initials-avatar.tsx` exports `InitialsAvatar` component with `userId`, `displayName`, `size` props
    - `apps/frontend/src/components/ui/initials-avatar.tsx` contains `PALETTE` array with 8 colors matching UI-SPEC hex values
    - `apps/frontend/src/components/ui/initials-avatar.tsx` contains `role="img"` and `aria-label` (per accessibility baseline)
    - `apps/frontend/src/components/layouts/private-route.tsx` exports `PrivateRoute` component
    - `apps/frontend/src/components/layouts/private-route.tsx` contains `isHydrating` check that renders `Loader2` spinner with `aria-label="Loading"`
    - `apps/frontend/src/components/layouts/private-route.tsx` contains `encodeURIComponent` in redirect URL (per D-17)
    - `apps/frontend/src/components/layouts/auth-layout.tsx` exports `AuthLayout` with `GraduationCap` icon and `Card` component
    - `apps/frontend/src/components/layouts/app-layout.tsx` exports `AppLayout` with `Sidebar` import and `Outlet`
    - `apps/frontend/src/components/layouts/sidebar.tsx` exports `Sidebar` with nav items (Dashboard, Courses, Assignments, Gradebook) using Lucide icons
    - `apps/frontend/src/components/layouts/sidebar.tsx` contains `api.delete('/auth/logout')` in logout handler
    - `apps/frontend/src/components/layouts/sidebar.tsx` contains `min-h-[44px]` on nav items and logout button (WCAG touch targets)
    - `apps/frontend/src/components/layouts/sidebar.tsx` contains `aria-label="Account settings"` on settings icon link
    - All files use `@/` path aliases — no relative `../` across directory boundaries
  </acceptance_criteria>
  <done>All layout components created per UI-SPEC: AuthLayout (centered card with logo per D-12), AppLayout (sidebar + content area), Sidebar (logo, nav items with active state styling, user info with InitialsAvatar, settings link, logout button per D-16), PrivateRoute (hydration spinner per D-18, redirect to /login?next= per D-17), InitialsAvatar (deterministic color from user ID per D-22, 8-color palette, accessible). All WCAG touch targets met (44px minimum).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store (memory) | Access token held in JavaScript memory — cleared on page unload |
| axios interceptor | Handles 401 responses; must not create infinite refresh loops |
| ?next= redirect param | User-controlled URL parameter — must validate to prevent open redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Information Disclosure | access token storage | mitigate | Token stored in Zustand memory only (per D-01). Never persisted to localStorage or sessionStorage. Cleared on page unload. |
| T-02-15 | Denial of Service | axios interceptor infinite loop | mitigate | Guard: `originalRequest.url === '/auth/refresh'` prevents refresh endpoint 401 from triggering another refresh. `_retry` flag prevents double retry. |
| T-02-16 | Tampering | ?next= open redirect | mitigate | PrivateRoute uses `encodeURIComponent(location.pathname + location.search)` — path-only, no external URLs. Login page validates: must start with `/`, must not contain `://`. |
| T-02-17 | Information Disclosure | Vite proxy in production | accept | Proxy is dev-only (Vite dev server). Production deployment uses reverse proxy (nginx/Vercel). No security impact. |
</threat_model>

<verification>
- `components.json` exists in `apps/frontend/` (shadcn initialized)
- All shadcn components exist in `apps/frontend/src/components/ui/`
- All layout components exist in `apps/frontend/src/components/layouts/`
- `pnpm --filter frontend build` compiles without TypeScript errors (once App.tsx is updated in Plan 04)
- No `any` type in any file
- No `localStorage` or `sessionStorage` usage for tokens
</verification>

<success_criteria>
1. shadcn/ui initialized with all 8 required components
2. All frontend packages installed (tanstack-query, zustand, react-hook-form, zod, axios, lucide-react, fontsource-inter, sonner)
3. Zustand auth store manages user/token/hydration state (AUTH-03)
4. Axios interceptor handles silent refresh with queue pattern (AUTH-03)
5. PrivateRoute prevents unauthenticated access with hydration spinner (AUTH-03, D-17, D-18)
6. AuthLayout provides centered card layout for auth pages (D-12)
7. AppLayout + Sidebar provide authenticated shell (D-16)
8. InitialsAvatar renders deterministic colored circles (D-22)
9. Vite proxy configured for API routing
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
